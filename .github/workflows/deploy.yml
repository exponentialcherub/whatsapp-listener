on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  deploy:
    uses: exponentialcherub/deploy-pi/.github/workflows/deploy-pi.yml@v1.0.5
    with:
      app-name: "whatsapp-listener"
      startup-cmd: "'npm install; npm run build; npm start;'"
      branch: "main"
    secrets: inherit
